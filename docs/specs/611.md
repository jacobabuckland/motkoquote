# REFUND-1: Full and partial refund on a settled job

## Problem

Currently there is no refund path in the product. With Pay by Bank becoming the default payment method and having no dispute mechanism, once a payment is settled it's irreversible inside motko — no chargeback, no refund. Stripe supports Pay by Bank refunds (full and partial) for up to 730 days, so this is a build limitation, not a platform constraint.

## Scope: what changes

Add refund capability for settled jobs that processes refunds through Stripe:
- Check eligibility: job must be settled via Stripe, not manually marked paid
- Process full or partial refunds through the Stripe API
- Show balance consequences before confirmation (money leaves trade's account, may go negative)
- Mark jobs with distinct refund state (settled → refunded or partially_refunded)
- Track refund history to enforce total-refunded-never-exceeds-settlement
- Idempotent: repeated refund of same amount does not move money twice

## Explicitly out of scope

- Staged jobs (deposit + final invoice) — handled by REFUND-2
- Policy limits on who may refund or time windows
- settlement-reversal.ts cleanup — CLEAN-5
- tests/setup.ts — not to be touched by this item

## Files

- src/lib/refund-settlement.ts (new) — core refund logic: eligibility check, Stripe API call, state update
- src/app/jobs/[id]/refund-actions.ts (new) — server actions for refund operations, with the session check
- src/app/jobs/[id]/refund-button.tsx (new) — UI component with confirmation dialog
- src/app/jobs/[id]/page.tsx (modify) — integrate refund UI into job detail page
- supabase/migrations/00000000000068_refund_settlement_states.sql (new) — widen
  `jobs.settlement_state` to admit `refunded` and `partially_refunded`, and add
  `jobs.total_refunded_pennies`
- tests/acceptance/611.test.ts (new) — acceptance tests

## Edge cases

**Connected account is empty** — Common case: trade has been paid out. Refund proceeds and account goes negative. The pre-confirmation warning makes this acceptable rather than a surprise.

**Multiple partial refunds** — Total refunded is tracked and never exceeds the original settlement. Each partial refund reduces the remaining refundable amount.

**Customer's bank rejects return** — Reported to the trade via error message, not swallowed. The job state reflects the attempt; manual resolution required.

**Manually-marked-paid job** — Has no Stripe payment_provider_ref to refund against. Eligibility check returns ineligible with clear reason: "No settlement to reverse (manual payment)".

**Already fully refunded** — Eligibility check returns ineligible: "Job already fully refunded".

**Idempotency** — Same refund request (job + amount) within a short window uses Stripe's idempotency keys. Repeated submission does not double-charge.

**Job never settled** — Eligibility check returns ineligible: "Job not settled".

## Risks

- **Money movement** — refunds debit the trade's Stripe connected account, potentially leaving it negative. The pre-confirmation warning is critical.
- **Stripe API failure** — must be surfaced to the trade, not buried. Partial state (fee reversed but Stripe call failed) must be impossible.
- **Audit trail** — refund history must be queryable for support and accounting.

## Derived criteria

What the acceptance tests verify:

1. **Eligibility for a settled Stripe job** — a job with `paid_at` set and a `pi_…` `payment_provider_ref` returns eligible with the correct `maxRefundablePennies`
2. **Not eligible for manual payment** — a job marked paid manually (no `payment_provider_ref`) returns ineligible, naming the manual payment
3. **Not eligible on another rail** — a TrueLayer `tl_…` settlement is refused rather than handed to Stripe
4. **Not eligible for an unsettled job** — a job with no `paid_at` returns ineligible
5. **The ceiling comes from Stripe** — `maxRefundablePennies` tracks what Stripe reports captured, not any figure held on our own rows
6. **Full refund processes** — calls Stripe `refunds.create` with the right payment intent and amount, and moves `settlement_state` to `refunded`
7. **Partial refund processes** — moves state to `partially_refunded` and leaves the remainder refundable
8. **The money comes out of the trade's account** — the refund carries `reverse_transfer: true`, so the destination charge's transfer is reversed rather than the platform absorbing it
9. **The motko fee is not returned** — `refund_application_fee` is false and no fee column is written, per `REVERSAL_CLAUSE.serviceFee`
10. **Multiple partials up to the total** — two partial refunds together equalling the settlement succeed; a third is refused
11. **Idempotent** — a repeat is refused outright by the recomputed ceiling, and the Stripe call carries an idempotency key for a retried request
12. **A rejected return reaches the trade** — a Stripe failure is reported, not swallowed
13. **Integration flow** — end to end: check eligibility → refund → state updated

All mocks (Supabase, Stripe) are built inside the test file, not in tests/setup.ts. The
module takes them as an optional argument, so the tests inject rather than mock the
module registry.

### Two things the derivation got wrong, recorded so the next reader does not repeat them

- **`settlement_state = 'settled'` is not a state this tree ever writes.** A settlement is recorded by `jobs.paid_at`; `settlement_state` is null until something goes backwards. Gating eligibility on `'settled'` produced a refund control that could never appear, and the migration therefore does not add that value.
- **`jobs.sent_total` does not exist.** `sent_total` is a column on `quotes` (migration 51). The settled amount is read from Stripe instead, in pennies — `invoices.amount` and `quotes.total` are numeric POUNDS, and a unit conversion inside a refund path is a 100x error.
