# Issue #101: Update STATUS.md and README: TrueLayer not Stripe, multi-provider LLM, SMS wired

## Problem

STATUS.md and README describe three aspects of the system incorrectly: they state that payments run on Stripe, that the LLM provider is OpenAI/Claude-only, and that SMS is not wired. All three claims are false. Payments moved to TrueLayer (VRP for fee collection, payment initiation for customer payments), the LLM path is multi-provider (Anthropic for text generation, OpenAI for voice and embeddings), and SMS is live via Twilio with full implementation. Meanwhile, stripe_* columns remain in the schema with zero code references — they are dead columns awaiting removal. Agents read repository documentation to scope work, so stale documentation is wrong instructions issued at scale.

## Scope

**What changes:**

1. **README.md** — update the Stack table (lines 10-22):
   - Row 18: Change `LLM | Claude API (Sonnet for quoting, Haiku for cheap tasks)` to describe the multi-provider setup accurately (Anthropic for quote drafting and chasing, OpenAI for voice and embeddings)
   - Row 19: Change `Payments | Stripe (Invoicing + Payment Links)` to `Payments | TrueLayer (VRP for fee collection, Payment Initiation for customer payments)`
   - Row 21: Change `Email/SMS | Resend + Twilio` to explicitly state that SMS is wired via Twilio (currently implies it may not be active)

2. **docs/STATUS.md** — correct section 2 "Stack (as actually built)" (lines 18-35):
   - Row 27: Update the LLM row to reflect multi-provider usage (currently states only `Claude Sonnet 4.6`)
   - Row 29: Change `Payments | Stripe (invoices + payment links + webhook)` to `Payments | TrueLayer (VRP for fee collection, Payment Initiation for customer payments)`
   - Lines 107-109: Remove or correct the statement "Twilio SMS chase is scaffolded in env but the implemented chase path is email (Resend); SMS is not wired." — SMS *is* wired per src/lib/sms.ts

3. **docs/STATUS.md** — add a note about dead stripe_* columns:
   - In section 3 "Feature status" → subsection "Data model", add a note that stripe_payment_link_id, stripe_payment_link_url, stripe_invoice_id, stripe_account_id, stripe_charges_enabled, stripe_payouts_enabled, and stripe_requirements_due columns exist in the schema with zero code references and are dead pending removal

4. **.github/pull_request_template.md** — create a new PR template containing:
   - A checklist item requiring documentation updates (STATUS.md, README.md) when a provider, integration, or externally-visible capability changes
   - Standard checklist items appropriate for this repository

**Files expected to change:**
- README.md
- docs/STATUS.md
- .github/pull_request_template.md (new file)

## Explicitly out of scope

- Do NOT remove stripe_* columns from the schema (that is a separate migration item)
- Do NOT remove any Stripe-related code or dependencies (e.g., in package.json)
- Do NOT restructure or reformat STATUS.md or README.md beyond the specific corrections
- Do NOT document roadmap items, planned work, or anything not currently implemented
- Do NOT update AGENTS.md or CLAUDE.md (those carry behavioral rules and change separately)

## Edge cases

- **A claim cannot be verified against code**: Remove the claim rather than guessing. Report what was removed in the PR description.
- **Documentation describes a feature that is partially implemented**: State explicitly what works and what does not. Do not describe partial features as complete.
- **Other stale claims found**: Correct them if verifiable against code. List each correction in the PR description with the supporting file and line reference.
- **Dead code or dependencies**: Mark as dead; do not remove (that's a separate cleanup item).

## Risks

**Low risk — documentation-only change:**
- No database migrations
- No code changes
- No user-facing changes
- No money-touching logic affected

**Verification required:**
- Every factual claim added must be verified by reading the actual source code
- The PR description must list the file and line number supporting each corrected claim

## Supporting evidence

**TrueLayer is the payment provider:**
- src/lib/truelayer-payments.ts:1-9 — Payment initiation for customer payments
- src/lib/truelayer-vrp.ts:1-11 — VRP for fee collection from trades
- 39 files reference "truelayer" (grep result)
- Zero TypeScript/TSX files reference stripe_* columns (grep result)

**LLM is multi-provider:**
- src/lib/claude.ts:5 — Anthropic client for quote drafting
- src/lib/chase.ts:4 — Anthropic client for chase copy
- src/lib/realtime.ts:23-24 — OpenAI for realtime voice API
- src/lib/embeddings.ts:7 — OpenAI for embeddings

**SMS is wired via Twilio:**
- src/lib/sms.ts:1-118 — Full implementation of sendQuoteSms and sendChaseSms
- src/app/api/twilio/inbound/route.ts — Inbound SMS webhook handler
- 18 files reference "twilio" (grep result)

**stripe_* columns are dead:**
- Schema columns exist in migrations/00000000000001_init_schema.sql:92 and migrations/00000000000006_invoices_payments.sql:2-3 and migrations/00000000000021_stripe_connect_payouts.sql:5-15
- Zero TypeScript/TSX files reference any stripe_* column (grep results: no matches in **/*.ts or **/*.tsx)

## Derived criteria

I understood the human to be asking for:

1. ✓ Correct STATUS.md to state TrueLayer (VRP + payment initiation) as the payment provider, not Stripe
2. ✓ Correct README.md to state TrueLayer as the payment provider, not Stripe
3. ✓ Update STATUS.md to describe the LLM path as multi-provider (Anthropic + OpenAI) with their respective uses
4. ✓ Update README.md to describe the LLM path as multi-provider
5. ✓ Correct STATUS.md to state that SMS is wired via Twilio (remove the false claim that it's not wired)
6. ✓ Add a note in STATUS.md marking stripe_* columns as dead with zero code references
7. ✓ Create .github/pull_request_template.md with a checklist item for documentation updates when providers/integrations change
8. ✓ Verify every claim against code and list supporting file:line references in the PR description
9. ✓ Ensure grep for "stripe" returns only schema column names and the dead-column note (no code references)
