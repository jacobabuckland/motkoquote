# Harness backlog

Deferred failures go here while one case is honing.

When `harness/NEXT_FIX.md` is locked (`status: open` or `honing`), do not start a second case. Note other findings below and pick from this list only after the locked case is `clean` and NEXT_FIX is `idle`.

<!-- format: - YYYY-MM-DD · caseId · severity · one-line failure · resultFile -->
